n = int(input())  # 輸入隊伍數量，並使其變為數字
a = []  # 儲存每個隊伍的人數

for _ in range(n):  # 迴圈讀取每個隊伍的人數
    a.append(int(input()))  # 將每個隊伍的人數加入列表中

a = 0  # 初始的隊伍數量為0

for count in a:  # 迴圈遍歷每個隊伍的人數
    if count % 3 == 0 and count != 0:  # 如果隊伍人數是3的倍數且不為0
        a += 1  # 將符合標準的隊伍數量加1

print(a)  # 輸出答案
